-- LSP related plugin configuration
-- - For simple plugin configs, we do it directly in this file.
-- - For configs which are a little more involved, we have a separate file 
--   which we 'require' here.

require "lsp_configs.luasnip"
require "lsp_configs.lspkind"
require "lsp_configs.nvim-cmp"
require "lsp_configs.nvim-lspconfig"
